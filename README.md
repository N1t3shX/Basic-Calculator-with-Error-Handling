# Basic Calculator with Error Handling

A simple **Python command-line calculator** that performs basic arithmetic operations while handling common runtime errors such as invalid input and division by zero.

This project is ideal for beginners to understand Python functions, loops, and exception handling.

---

## 🚀 Features

- ➕ Addition  
- ➖ Subtraction  
- ✖ Multiplication  
- ➗ Division  
- ❌ Handles invalid (non-numeric) inputs  
- ⚠ Prevents division by zero  
- 🔁 Runs continuously until the user exits  

---

## 🧠 How It Works

1. Displays a menu of arithmetic operations.
2. Takes user input for operation choice and numbers.
3. Uses `try` and `except` blocks to catch errors.
4. Displays results or appropriate error messages.
5. Repeats until the user chooses to quit.
